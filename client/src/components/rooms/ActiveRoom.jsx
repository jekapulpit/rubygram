import React from 'react';
import { hot } from 'react-hot-loader/root';
import '../../stylesheets/components/rooms.scss'

const ActiveRoom = props => {
    return (
        <div className='content-container'>

        </div>
    )
};

export default hot(ActiveRoom);